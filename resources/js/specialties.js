// JS for minimalistic specialties component
// No complex scroll jacking needed here anymore, keeping it clean and fast.
document.addEventListener('DOMContentLoaded', () => {
    // any future lightweight interactions can go here
});
